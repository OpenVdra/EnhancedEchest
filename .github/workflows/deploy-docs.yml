name: Deploy VitePress docs to GitHub Pages

on:
  # Documentation is a living site: deploy the exact main-branch commit whenever
  # its docs (or this workflow) change. A manual run deploys the selected branch's
  # current commit instead of silently falling back to an older release tag.
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/deploy-docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    if: github.repository == 'OpenVdra/EnhancedEchest'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: docs/package-lock.json

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: docs/node_modules
          key: ${{ runner.os }}-node-22-modules-${{ hashFiles('docs/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-22-modules-

      - name: Install dependencies
        working-directory: docs
        run: npm ci

      - name: Generate Javadocs
        run: |
          chmod +x gradlew
          ./gradlew javadoc --no-daemon

      - name: Build VitePress
        working-directory: docs
        run: npm run docs:build

      - name: Add Javadocs to the Pages site
        run: |
          mkdir -p docs/.vitepress/dist/javadocs
          cp -R build/docs/javadoc/. docs/.vitepress/dist/javadocs/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist

  deploy:
    if: github.repository == 'OpenVdra/EnhancedEchest'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
