# Build metadata baked in at build time by Gradle's processResources.
# 'dev' is true only for local dev runs (./gradlew runServer) and false in the
# jar shipped to users (build/shadowJar). It gates developer-only logging.
dev=${devBuild}
