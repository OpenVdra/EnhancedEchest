# FastStats project token. The value below is a build-time placeholder — Gradle's
# processResources replaces ${faststatsToken} with the real token (from the
# FASTSTATS_TOKEN env var or a gitignored secrets.properties) when the jar is built.
# It is intentionally empty in source control; an empty value makes the plugin skip FastStats.
token=${faststatsToken}
